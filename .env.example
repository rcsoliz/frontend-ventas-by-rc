# URL del endpoint GraphQL del backend (Django + Strawberry).
# El codegen (npm run codegen) también apunta acá vía introspección — el backend debe estar corriendo.
VITE_GRAPHQL_URL=http://localhost:8000/graphql/
